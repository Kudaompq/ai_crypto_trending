# 图表显示问题诊断指南

## 🔍 问题诊断步骤

### 1. 打开浏览器开发者工具

按 `F12` 或右键点击页面选择"检查"

### 2. 查看控制台 (Console)

在控制台中查找以下日志信息：

#### 正常情况应该看到：
```
Initializing chart...
Container dimensions: 1200 x 500
Candles data: 100 candles
Chart created successfully
Candlestick series added
Updating chart with 100 candles
First candle: {time: ..., open: ..., high: ..., low: ..., close: ...}
Last candle: {time: ..., open: ..., high: ..., low: ..., close: ...}
Chart data set successfully
Updating SR levels...
Adding 4 resistance levels
Resistance 1: $3094.62 (strength: 0.4)
Resistance 2: $3481.04 (strength: 0.4)
...
Adding 1 support levels
Support 1: $2872.91 (strength: 0.4)
SR levels updated. Total price lines: 5
```

#### 如果看到错误：

**错误1**: `Chart container not found`
- **原因**: DOM 元素未正确挂载
- **解决**: 检查组件是否正确渲染

**错误2**: `Cannot update chart: series or candles missing`
- **原因**: 数据未传递到组件
- **解决**: 检查 API 调用是否成功

**错误3**: `Error initializing chart: ...`
- **原因**: TradingView 库加载失败
- **解决**: 检查网络连接，重新安装依赖

### 3. 检查网络请求 (Network)

1. 切换到 Network 标签
2. 刷新页面
3. 查找 `/api/analysis` 请求
4. 确认返回状态码为 200
5. 查看响应数据中的 `sr_levels`

### 4. 检查元素 (Elements)

1. 切换到 Elements 标签
2. 找到 `.kline-chart-container`
3. 确认内部有 `<div class="chart">` 元素
4. 检查容器是否有宽度和高度

---

## 🛠️ 常见问题解决方案

### 问题1: 图表完全不显示

**可能原因**:
1. TradingView 库未正确加载
2. 容器尺寸为 0
3. 数据未传递

**解决方案**:
```bash
# 重新安装依赖
cd frontend
npm install lightweight-charts --save
npm run dev
```

### 问题2: 图表显示但没有K线

**可能原因**:
1. API 返回数据为空
2. 时间戳格式错误
3. 数据格式不正确

**检查方法**:
```javascript
// 在浏览器控制台输入
fetch('http://localhost:8080/api/kline?symbol=ETHUSDT&interval=1d&limit=10')
  .then(r => r.json())
  .then(d => console.log(d))
```

### 问题3: 支撑位不显示

**可能原因**:
1. 支撑位价格超出图表可见范围
2. 透明度过低
3. 价格线未正确创建

**解决方案**:
1. **缩放图表**: 使用鼠标滚轮缩小查看更大范围
2. **检查数据**: 
   ```javascript
   // 在控制台查看支撑位数据
   console.log('Support:', window.store?.analysisResult?.sr_levels?.support)
   ```
3. **手动调整视图**: 在图表上拖动查看不同价格区间

---

## 📊 预期显示效果

### K线图
- 绿色蜡烛: 上涨
- 红色蜡烛: 下跌
- 时间轴在底部
- 价格轴在右侧

### 支撑/压力位
- **压力位** (红色虚线):
  - $3094.62
  - $3481.04
  - $3665.29
  - $3885.01
  
- **支撑位** (绿色虚线):
  - $2872.91 (可能在图表下方，需要缩放查看)

---

## 🔧 快速修复命令

```bash
# 1. 停止前端服务器 (Ctrl+C)

# 2. 清除缓存并重新安装
cd frontend
rm -rf node_modules package-lock.json
npm install

# 3. 重新启动
npm run dev

# 4. 在浏览器中硬刷新 (Ctrl+Shift+R)
```

---

## 📞 获取帮助

如果问题仍然存在，请提供以下信息：

1. 浏览器控制台的完整错误日志
2. Network 标签中 `/api/analysis` 的响应数据
3. 浏览器版本和操作系统
4. 截图显示当前页面状态

---

**更新时间**: 2025-11-29 13:45
