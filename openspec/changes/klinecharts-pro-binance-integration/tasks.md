## 1. 验证 Pro/Core 集成边界

- [x] 1.1 在隔离验证页中用候选 Pro/Core 组合验证 Vue 容器挂载、销毁、尺寸变化、切换交易对和周期。
- [x] 1.2 验证自定义 Datafeed 的历史时间范围、左侧翻页触发、SSE 订阅取消和错误回调能否表达现有行情语义。
- [x] 1.3 验证内置指标入口可注册自定义 OI 副图，且绘图对象可显式导出、恢复和移除；记录可用 API 与最终锁定版本，缺失能力时先实现最小显式适配接口。

## 2. 建立行为测试并观察 Red

- [x] 2.1 检查前端现有自动化测试配置；若没有可重复测试入口，添加最小前端测试运行器和 package script。
- [x] 2.2 为 OI 适配增加字段值不同的固定样本测试，断言绘图值来自 `sumOpenInterest`、不来自 `sumOpenInterestValue`，并运行观察到行为断言失败。
- [x] 2.3 为 Binance Datafeed 添加历史周期映射、边界去重、取消订阅和切换后忽略旧响应的行为测试，并运行观察到待实现行为失败。
- [x] 2.4 为绘图偏好迁移添加旧 Core 对象往返、按交易对隔离、幂等迁移和失败保留旧存档的行为测试，并运行观察到待实现行为失败。

## 3. 锁定依赖并接入图表外壳

- [x] 3.1 按验证结果固定兼容的 Pro/Core 版本，更新 manifest 与 lockfile，并运行图表相关构建检查。
- [x] 3.2 实现 Vue 生命周期图表外壳，验证挂载、销毁、尺寸变化及切换交易对/周期时旧图表实例不会继续接收事件。
- [x] 3.3 使用集成周期栏呈现并选择 15 个 Binance 原生周期，验证选择后当前标的加载相应周期且不发生客户端聚合。

## 4. 接入 Binance 历史和实时 Datafeed

- [x] 4.1 实现仅返回项目支持 USDⓈ-M 标的的交易对搜索与 15 周期映射，并复用现有历史 API 获取指定区间数据。
- [x] 4.2 将 Datafeed 历史请求接入向左分页，验证按开盘时间升序合并、边界去重、hasMore 终止及失败后可重试。
- [x] 4.3 将现有 SSE/store 事件桥接到 Pro 图表，校验当前交易对、周期和请求代次，并在取消订阅时释放监听。
- [x] 4.4 保留实时与 REST 备用状态协调，验证只有收到匹配的有效实时 K 线才显示实时，备用更新明确显示为非实时。

## 5. 修正 OI 并保留指标偏好

- [x] 5.1 修改 OI 映射和图例，使副图只使用 `sumOpenInterest` 数量及对应时间，并在无数据或不支持周期时保持空序列。
- [x] 5.2 将 OI 注册到集成指标入口，保留可恢复错误、交易对切换隔离及跨标的/周期共享选择。
- [x] 5.3 将 MA、EMA、BOLL、MACD 和 OI 配置接入集成指标入口，保留现有参数校验、共享范围及旧偏好迁移规则。
- [x] 5.4 运行第 2 组 OI、Datafeed 和偏好测试，确认行为断言由 Red 转为 Green。

## 6. 迁移绘图并替换自定义控件

- [x] 6.1 将 Pro 绘图入口接入直线/射线/线段、通道、形状、斐波那契和波浪类别，并验证选择工具、放置锚点、编辑、吸附、隐藏、锁定及删除交互。
- [x] 6.2 实现 Core 旧绘图对象到 Pro 绘图状态的版本化、幂等迁移；成功写入前保留旧存档，逐交易对验证恢复和隔离。
- [x] 6.3 仅替换右侧图表区域内的 `interval-buttons`、`chart-header`、`chart-summary` 和 `chart-toolbar`，由 Pro 周期、指标和绘图控件接管对应操作；保留左侧 Watchlist、页面主 header、全局行情状态及其他区域的 header。
- [x] 6.4 移除自定义当前价、相邻 K 线涨跌幅、高低价、加载区间成交量及 ATR 摘要，验证 Watchlist 报价和全局行情状态仍显示。

## 7. 回归与实际使用路径验收

- [x] 7.1 运行前端自动化测试、`npm run build` 和 `git diff --check`，记录每条命令的结果及失败项。
- [x] 7.2 在实际页面切换交易对和周期，验证历史分页、旧响应隔离、共享指标、逐标的绘图恢复和 OI 数量显示。
- [x] 7.3 通过实际后端 SSE 采样确认连续收到多条匹配当前标的与周期、事件时间推进的有效 K 线；另外验证断流/备用状态和恢复后状态。
- [x] 7.4 在浏览器验证实时状态只在有效 K 线到达后出现、图表行情时间持续更新，且切换标的后没有旧标的数据串入。
