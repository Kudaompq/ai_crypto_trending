## 1. 建立基线并先写失败测试（TDD Red）

- [x] 1.1 运行当前前端测试、`npm run build` 和 `go test -count=1 ./backend/...`，记录可复现的基线结果。
- [x] 1.2 为 K 线历史 API 添加行为测试：可选 `endTime` 游标会返回游标之前的数据、缺省游标保持最新数据行为、非法游标返回 400、分页结果按时间升序且最多返回 500 条；先运行并观察对应行为断言失败。
- [x] 1.3 为 K 线 REST 与 SSE 周期校验添加测试：覆盖全部 Binance USDⓈ-M 原生周期、拒绝未知周期，并确认非法周期不会建立上游订阅；先观察行为断言失败。
- [x] 1.4 为前端图表和 Dashboard 添加行为测试：KLineChart 周期映射、周期控件位于图表上方、选择周期重新加载正确历史与 SSE、旧响应不覆盖新周期、翻页去重/结束/失败重试及卸载取消订阅；先观察行为断言失败，不以导入或编译失败作为 Red 证据。

## 2. 实现后端周期校验与历史分页

- [x] 2.1 为 K 线 API、service 和 repository 添加可选毫秒级 `endTime` 游标，透传至 Binance 数据请求；校验非法值并维持缺省游标的兼容行为。
- [x] 2.2 扩展 K 线历史响应以报告 `has_more_before`，保证数据按开盘时间升序；分页边界不重复已返回 K 线，且达到历史起点后停止向左加载。
- [x] 2.3 统一 REST 与 SSE 的 Binance USDⓈ-M 原生周期白名单，支持 `1m`、`3m`、`5m`、`15m`、`30m`、`1h`、`2h`、`4h`、`6h`、`8h`、`12h`、`1d`、`3d`、`1w` 和 `1M`，非法周期返回可识别的 400 错误。
- [x] 2.4 运行后端新增周期、游标和边界测试，确认 RED 用例转为通过，并运行相关后端回归测试。

## 3. 替换图表并接入 KLineChart 数据加载器

- [x] 3.1 添加并锁定 `klinecharts` 依赖，移除 `lightweight-charts` 依赖，更新 lockfile，并审阅需要保留的 LICENSE/NOTICE 信息。
- [x] 3.2 新建 KLineChart Vue 组件：将当前 OHLCV 蜡烛数据映射到毫秒时间戳结构，保留现有价格、涨跌、高低、成交量及 ATR 信息，并在卸载时销毁图表和尺寸观察器。
- [x] 3.3 实现 KLineChart 历史 `getBars` 适配：初次加载最近一页；向左翻页时使用最早 K 线开盘时间之前的 `endTime`，按响应 `has_more_before` 决定是否继续，并验证排序、去重、失败重试及旧请求隔离。
- [x] 3.4 将现有 K 线 SSE 接入 KLineChart `subscribeBar`/`unsubscribeBar` 生命周期，保证每个当前交易对和周期只有一条浏览器订阅；周期/交易对切换和组件卸载时关闭旧订阅，保留现有备用更新及非实时状态。
- [x] 3.5 将 Dashboard 周期控件从 header 移到图表绘图区上方，显示支持的 Binance 原生周期，并映射 `1m`、小时、日、周和 `1M` 月周期到 KLineChart `{ span, type }`；不添加任意周期输入或聚合逻辑。
- [x] 3.6 移除旧图表实现、Lightweight Charts 导入和依赖，以及会错误归属新图表的 TradingView 专属说明；确认 Watchlist 与移除分析面板后的页面边界不变。

## 4. Green 与端到端验收

- [x] 4.1 运行前端完整测试和 `npm run build`，运行 `go test -count=1 ./backend/...` 及 `git diff --check`；确认新增行为测试通过。
- [x] 4.2 在本地可控数据源验证分页游标前后无重叠、无缺序，短页/空页停止加载，失败后可重试，切换周期后旧请求及事件不会覆盖新数据。
- [x] 4.3 启动应用并在真实浏览器验证 KLineChart 缩放/拖动、向左加载更早历史、周期控件位置、原生周期切换及 SSE 更新；观察至少两个周期和两个 Watchlist 交易对，确认行情与选择一致、旧订阅关闭。
- [x] 4.4 确认生产依赖中不再包含 Lightweight Charts，页面没有旧库专属误导性标识，并核对 KLineChart 所需许可信息。
