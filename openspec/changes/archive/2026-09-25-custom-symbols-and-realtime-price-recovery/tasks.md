## 1. 统一交易对校验

- [x] 1.1 增加 Binance 合约交易对规范化、格式校验与可交易状态查询，使用短时缓存并区分无效、不可交易和上游暂不可用。
- [x] 1.2 增加供前端添加自选交易对使用的校验 API，并让 K 线、分析、交易机会及 SSE 入口共用校验结果和错误语义。
- [x] 1.3 覆盖校验 API 与现有行情入口的关键场景：大小写归一、无效格式、不可交易、数据源故障及有效自选币种。

## 2. 自选交易对界面

- [x] 2.1 在前端 store 中合并预设交易对和浏览器持久化自选列表，处理重复项、损坏存储及写入失败。
- [x] 2.2 在交易对选择区加入手动添加和删除自选交易对的交互，展示校验中的状态及三类错误反馈。
- [x] 2.3 修正交易对或周期快速切换时的异步响应竞争，停止旧订阅并阻止旧行情、分析和交易机会覆盖当前页面。

## 3. 实时行情状态与恢复

- [x] 3.1 扩展后端 SSE 状态事件，报告上游连接失败与重试，同时保留 `ready` 和 `kline` 事件；确认动态交易对可以订阅。
- [x] 3.2 将前端行情状态改为正在连接、实时更新、备用更新和数据不可用，仅收到匹配的 `kline` 后进入实时更新，并展示最近接收时间。
- [x] 3.3 在约 15 秒无实时事件后启动无并发的约 15 秒 REST 备用行情更新；恢复实时事件时停止备用轮询，切换交易对时清理旧定时器与请求结果。
- [x] 3.4 验证仅收到 `ready`、上游握手超时、断流、REST 失败、实时恢复及参数校验失败时的状态与数据行为。

## 4. 集成验证与说明

- [x] 4.1 验证自选交易对从添加到 K 线、分析、交易机会及实时行情的完整路径，以及刷新页面后的列表恢复。
- [x] 4.2 在上游 WebSocket 可用和不可用两种环境分别验证价格更新、备用更新及错误提示，并更新 README 中的使用与排障说明。
