## 1. 建立基线并先写失败测试（TDD Red）

基线记录：前端 4 个测试文件、7 项测试通过；前端构建因本地未安装 `vue-tsc` 失败；`go test -count=1 ./backend/...` 通过。

- [x] 1.1 记录当前前端测试、前端构建和 Go 后端测试基线结果。
- [x] 1.2 添加 Watchlist 前端行为测试：显示预设和自选交易对、每行价格、当前项高亮、选中项切换 K 线、添加和删除入口位于 Watchlist，且 header 不再有币种下拉；运行测试并观察对应行为断言失败。
- [x] 1.3 添加前端报价状态测试：快照初始化、多个 symbol 的更新隔离、较旧快照不覆盖较新 SSE 报价、加入/删除列表后订阅更新、断流标 stale 及恢复；运行测试并观察行为断言失败。
- [x] 1.4 添加后端报价服务和路由测试：本地 WebSocket 数据源注入、全市场 ticker 数组解析、仅接受 USDⓈ-M、按各客户端 Watchlist 过滤、最新价快照过滤、无效交易对拒绝及 SSE 取消清理；运行测试并观察行为断言失败，而非导入或编译失败。
- [x] 1.5 添加 Lightweight Charts 组件行为测试：历史 OHLCV 映射、实时蜡烛更新、周期/交易对切换、容器尺寸变化及卸载释放；必要时先添加最小依赖和可编译的组件接口，再运行测试并观察行为断言失败。

## 2. 实现后端 Watchlist 多币种价格流

- [x] 2.1 实现 Binance USDⓈ-M 全市场 mini ticker 上游连接、消息数组解析、价格数值校验、`st = 1` 类型过滤、重连及静默检测，并用本地 WebSocket 测试验证。
- [x] 2.2 实现可共享的报价分发服务，按 SSE 客户端各自请求的 Watchlist 过滤 symbol，覆盖多个客户端隔离、订阅取消和服务端资源清理。
- [x] 2.3 实现 Watchlist 最新价格快照接口，校验全部请求交易对，仅返回其最新价格；覆盖上游错误、空响应和非法数值。
- [x] 2.4 实现多币种价格 SSE 接口，发送快照、symbol/价格/事件时间、有效上游状态及重连状态；验证页面取消连接后订阅被释放。
- [x] 2.5 运行后端报价相关测试及并发/race 测试，确认 SSE ready 本身不会被当作已收到实时价格。

## 3. 实现前端 Watchlist 和报价展示

- [x] 3.1 在前端 API 层添加最新价格快照与 Watchlist 价格 SSE 类型和接口，并保持现有 K 线 REST/SSE 协议不变。
- [x] 3.2 在 store 中按 symbol 缓存快照和实时价格；按事件时间拒绝旧数据，保留最后已知价格并管理 connecting/live/stale 状态。
- [x] 3.3 新增 Watchlist 区域并移除 header 币种下拉；在列表内展示所有预设/自选交易对、各自最新价格、当前选中状态及价格流状态。
- [x] 3.4 将添加和删除操作移入 Watchlist，沿用现有格式化、Binance 校验、本地持久化及至少保留一个交易对的限制；添加币种和删除币种后更新报价订阅集合。
- [x] 3.5 选中 Watchlist 行时只切换当前图表的 symbol 和 K 线订阅；验证旧 K 线请求/推送不会覆盖当前选择，且报价更新不会混入其他币种。
- [x] 3.6 实现页面卸载、列表变化、重复挂载时的 EventSource 关闭与资源清理；对窄屏布局验证 Watchlist 移到图表上方。

## 4. 用 Lightweight Charts 替换手工 K 线

- [x] 4.1 添加 `lightweight-charts` 依赖并更新 lockfile；在图表区域保留 TradingView 归属链接。
- [x] 4.2 新建 Vue 图表组件，把现有毫秒时间戳和 OHLCV 映射为蜡烛图数据；历史加载使用完整数据更新，实时更新仅更新当前蜡烛或追加新蜡烛。
- [x] 4.3 接入现有 15 分钟、1 小时、4 小时、1 天控件，切换交易对/周期时替换图表数据并清理旧图表订阅。
- [x] 4.4 响应侧栏和视口尺寸变化，保留现有当前价格、涨跌幅、高低价、成交量和 ATR 信息；移除旧手写 K 线实现并更新相关测试。
- [x] 4.5 运行 Lightweight Charts 组件及 Dashboard 行为测试，确认交互缩放/拖动、实时 update、备用数据及卸载清理行为。

## 5. 完成端到端验收

- [x] 5.1 运行前端完整测试和 `npm run build`，运行 `go test -count=1 ./backend/...`、报价并发路径的 `go test -race` 及 `git diff --check`。
- [x] 5.2 用本地可控行情源验证报价流断开、重连、快照与实时事件乱序、无效行情和多客户端隔离；断连时保留价格但显示非实时。
- [x] 5.3 启动真实应用并验证 Watchlist 至少两个 USDⓈ-M 交易对的 SSE 报价连续到达且各行 symbol/价格匹配；在真实浏览器确认价格可见并更新、切换币种和周期正常、增删后订阅正确、离开页面后连接释放。
