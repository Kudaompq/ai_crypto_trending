## 1. 前后端行为测试与 Red 验证

- [x] 1.1 为当前没有组件测试入口的前端添加最小 Vue 组件测试工具、配置和 `test` 脚本。
- [x] 1.2 先编写 Dashboard 与 SimpleChart 行为测试：断言六类分析面板、交易机会入口/弹窗、后台轮询和支撑/压力位标记均不存在，同时 K 线仍显示；运行并确认对应断言因现有行为而失败。
- [x] 1.3 为生产路由和机会数据库清理编写 Go 行为测试：断言旧 API 返回 404，升级清理会删除临时目录中的旧机会数据库且不会重建；运行并确认测试因当前路由和数据库初始化行为而失败。

## 2. 移除前端展示与请求

- [x] 2.1 从 Dashboard 删除整个分析面板区域、交易机会按钮/弹窗、后台轮询/通知及相关状态和组件引用。
- [x] 2.2 从 SimpleChart 删除支撑/压力位摘要、水平线、相关 props、计算逻辑和样式，保留 K 线、价格信息及 ATR 展示。
- [x] 2.3 删除交易机会 Vue 组件和前端 API 方法、类型；清理对应的调用、样式与无用导入。

## 3. 移除后端交易机会链路并清理数据

- [x] 3.1 实现启动时对旧 `data/opportunities.db` 的幂等删除；数据库不存在时正常继续，删除失败时明确报错并停止启动，且不再初始化或重建机会数据库。
- [x] 3.2 删除 `/api/opportunities` 路由、handler、检测 service、repository、机会专用模型及无其他用途的数据库代码/依赖，并调整相关路由测试与服务端 endpoint 日志。

## 4. Green 与回归验收

- [x] 4.1 运行前后端新增行为测试，确认面板/交易机会功能已消失、旧 API 返回 404、旧数据库文件被删除且不会重建。
- [x] 4.2 在隔离的临时工作目录预置旧版机会数据库后启动后端，确认文件被清理且健康接口正常；不得用工作区真实数据库作为测试夹具。
- [x] 4.3 运行 `go test -count=1 ./backend/...`、前端组件测试与 `npm run build`，并验证 OpenSpec 变更通过严格校验。
