# Codex 项目维护约定

你是本项目的维护工程师。使用中文沟通，在理解现有行为的基础上完成用户任务。以代码、测试结果和实际运行证据为依据，避免把推测写成结论。用户当次明确指令优先于本文件。

## 优先执行的规则

本文件适用于整个仓库；子目录存在 `AGENTS.md` 时，同时遵循其适用规则。

1. 行为变更先写失败测试，再改实现，最后通过相关回归和实际使用路径验证。
2. 编译成功、健康检查成功或备用行情可用，都不足以证明实时行情问题已修复。
3. 验收存在失败、跳过或未验证项时，明确报告，不能标记“全部完成”。
4. 保留已有工作区修改，按任务范围持续推进；不为已授权的常规实现步骤反复请求确认。

## 项目背景与入口

本项目提供 Binance USDⓈ-M 合约交易对的 K 线、技术分析、交易机会和实时行情展示，支持浏览器本地保存自选交易对。

- 后端：Go + Gin，根目录 `go.mod` 管理依赖；SQLite 保存交易机会等数据。
- 前端：Vue 3 + TypeScript + Vite + Pinia + Element Plus。
- 行情链路：Binance WebSocket → Go 行情服务 → SSE → 前端 store 和图表；REST 提供历史数据、分析及备用更新。
- `backend/cmd/server/main.go`：服务初始化及路由。
- `backend/internal/handler/`：HTTP/SSE 输入校验、响应及错误语义。
- `backend/internal/service/`：业务逻辑、交易对校验及共享实时订阅。
- `backend/internal/repository/`：上游数据访问。
- `backend/internal/indicator/`、`model/`：指标计算与数据模型。
- `frontend/src/services/api.ts`：前端 API 和 SSE 接口。
- `frontend/src/stores/analysis.ts`：交易对、行情和分析状态。
- `frontend/src/views/Dashboard.vue`：主页面及行情状态协调。
- `openspec/specs/`：主规格；`openspec/changes/`：活跃变更；`openspec/changes/archive/`：历史变更。

文件路径、依赖版本、端口和命令以当前仓库为准；README 的历史描述可能落后于代码。

## 开始工作

1. 阅读适用的 `AGENTS.md`、相关代码、现有测试及涉及的 OpenSpec 规格。
2. 运行 `git status --short`，辨认已有修改。保留用户工作，不覆盖、不回滚无关文件。
3. 简要说明本次目标和可验证的验收条件；范围明确时直接推进。
4. 调查 bug 时先复现并收集证据，再判断原因。外部 API、SDK 和协议可能变化，遇到接口异常应核对当前官方文档和实际响应。

## TDD：所有后续行为修改的硬性要求

新增功能、修复 bug、修改配置导致的运行行为变化，必须执行以下流程；纯重构先建立现有行为的通过基线，再在测试保护下调整结构：

1. **Red**：先添加或调整自动化测试，运行并观察失败。失败必须对应待修复的行为或需求。
2. **Green**：只实现让测试通过所需的改动，再运行测试确认通过。
3. **Refactor**：必要时整理代码，保持测试通过，并完成相关回归验证。
4. **验收**：验证真实使用路径，报告实际结果，全部必需检查通过后才声明完成。

具体约束：

- 测试应断言外部可观察行为，例如订阅路径、收到的数据、超时后的状态和资源释放；避免仅检查某个常量或函数是否存在。
- 导入错误、缺失符号、测试环境故障不算有效的行为失败。必要时先补最小接口桩使测试可执行，再观察行为断言失败，之后实现业务逻辑。
- 不允许先改实现再补测试并称其为 TDD；不允许删掉断言、跳过失败用例或降低标准来获得通过结果。
- 重构前先建立覆盖现有行为的测试；纯文档、排版等无运行行为的修改，通过内容核对和差异检查验证，不编造运行测试。
- 网络协议测试优先使用本地 HTTP/WebSocket 服务和可控事件；普通单元测试不依赖 Binance 网络可用性。
- 并发及计时用例使用同步信号和可控超时，避免依靠长时间 sleep；覆盖取消、重连及旧响应覆盖新状态等边界。
- 编写本文件时，`frontend/package.json` 尚无自动化测试脚本，后续先检查当前配置。修改前端行为时，先建立与任务相称的可重复测试入口，并记录运行命令；浏览器人工观察不能替代 Red 阶段。优先沿用已有工具，确实缺少测试能力时再引入最小依赖。
- 记录失败与通过的命令及关键输出。若缺乏验证条件，明确标为“未完成验收”，不能声称已通过。

## 实现与依赖维护

- 遵循现有分层：handler 处理协议与输入，service 协调业务，repository 访问数据；组件负责呈现，store 管理共享状态，API 模块封装传输。
- 修复前定位实际调用链和锁定的依赖版本。检查 `go.mod`、`go.sum`、`frontend/package-lock.json`，不能以本机缓存中另一个 SDK 版本推断项目行为。
- 外部 SDK 升级前读取官方兼容性说明和实际使用到的实现；通过失败测试证明需求，升级后检查 REST、WebSocket、解析及错误语义的回归。避免为局部 bug 无依据地升级整套依赖。
- 依赖变更同步更新锁文件；不直接修改模块缓存或 `node_modules` 作为修复。
- 避免在并发请求或重复构造实例时写 SDK 全局配置；确需全局初始化时，明确初始化顺序并测试并发影响。
- 错误处理保留可定位的原因；数值解析失败、空数据、重复或乱序事件不能悄悄变成正常行情。
- 指标及交易机会算法变更应使用固定输入验证计算结果和边界，不凭页面观感判断正确性。

## 行情接入与状态约束

- K 线使用 Binance USDⓈ-M 市场数据入口 `wss://fstream.binance.com/market/ws`；维护时再次核对官方规范。
- 已知历史故障：旧 SDK 使用 `/ws`，连接能建立但 K 线没有数据。Binance 的 2026 年迁移公告要求 K 线走 `/market`。
- 官方参考：https://developers.binance.com/zh-CN/docs/products/derivatives-trading-usds-futures/websocket-market-streams/Important-WebSocket-Change-Notice
- WebSocket 握手成功、SSE `ready` 或浏览器 `onopen` 均不能作为实时行情正常的证据；只有收到匹配当前交易对和周期的有效 `kline` 才进入实时状态。
- REST 备用更新要明确标为非实时；备用路径成功不等于 WebSocket bug 已修复。
- “连接成功但无数据”必须继续排查端点、订阅参数、原始帧、解析过程和代理路径，不能无证据归因于网络。
- 保留重连、静默连接检测和备用行情能力。失败时展示真实状态，避免过期价格被标为实时。
- 切换交易对或周期必须清理旧订阅与定时器，并阻止旧请求或推送覆盖当前数据。
- 前后端共用一致的交易对规则，区分输入错误、不受支持和上游暂不可用；不得恢复为静态币种白名单。
- 不擅自替换为现货、测试网或其他数据源来使测试通过。

## 启动与重启

在项目根目录运行：

```bash
./start.sh
# 8080 被占用时使用：
PORT=18080 ./start.sh
```

- 前端默认 `http://localhost:5173`，后端默认 8080；脚本会根据 `PORT` 设置前端代理目标。
- 脚本编译后端并启动两个服务，Ctrl+C 清理所属服务。后端运行目录是 `backend/`，注意相对数据库路径。
- 重启前检查监听端口、进程命令和工作目录，仅停止本项目所属进程。不得为了释放端口停止其他项目或整个 Docker。
- 修改 Go 代码后重新编译并重启；Vite 热更新不能更新后端二进制。
- 重启后检查页面、直接后端健康接口，以及前端代理的 `/api/health`。检查本地服务时可使用 `curl --noproxy '*'`，不要把失效的本地代理误判为服务故障。
- Docker 端口以 `docker-compose.yml` 为准，避免混淆开发服务与容器服务。
- 用户要求启动服务供其测试时，交付前保持服务运行，报告实际访问地址；只清理本次临时测试进程和产物。

## 验证与完成标准

后端改动通常需要在根目录执行：

```bash
go test -count=1 ./backend/...
# 涉及共享订阅、goroutine、锁、通道等并发逻辑时：
go test -race -count=1 ./backend/internal/service ./backend/internal/handler
git diff --check
```

前端改动需运行对应自动化测试，并在 `frontend/` 执行：

```bash
npm run build
```

`npm run build` 包含 TypeScript 检查；`npm run build:docker` 仅执行 Vite 构建，不能代替类型检查。检查失败时记录失败项；如果确认是既有问题，说明基线证据和影响范围，不宣称全部检查通过，也不要顺手扩大修改范围。

验证按改动选择：文档核对内容与差异；后端运行相关行为测试及后端回归；前端运行行为测试、类型检查和构建；跨端变更额外运行端到端场景。不要因为命令列在此处就对无关改动机械执行全部检查。

实时行情相关改动还必须验证：

- 本地协议测试覆盖事件解析、断流重连、取消订阅和恢复行为。
- 实际后端 SSE 在限定采样时间内连续收到多条有效 `kline`，核对交易对、周期和事件时间是否持续推进；不能要求每帧价格必然变化。
- 实际浏览器显示“实时更新”，图表或最近行情时间持续变化，切换交易对后没有串数据。
- 对降级逻辑的修改，另验证仅 `ready`、静默、REST 失败以及恢复后的状态。
- 有时限的 SSE 采样可能由客户端主动超时结束；依据捕获的事件断言结果，不把非零退出码隐藏或直接视为服务失败。

全部必要验证通过后停止重复测试。最终简要说明根因、修改、Red/Green 证据、验收结果，以及仍存在的限制。出现失败就继续定位；无法解决时如实报告阻塞，不把模拟验证说成真实上游验证。

交付证据至少包含：行为失败用例及失败原因、修复后运行的命令与结果、涉及界面或上游时的实际验证结果。测试不得只存在于会话中，自动化回归用例应保存在仓库。网络恢复后的单次成功不能抹去此前失败记录；说明观察范围，避免宣称已证明长期稳定。

## OpenSpec 与变更边界

- 用户调用 OpenSpec skill 时，读取并遵循对应 `.agents/skills/*/SKILL.md`。
- 根据当前变更的 proposal、specs、design 和 tasks 实现；任务完成标记必须有测试及验收证据。
- 已归档变更作为历史记录保留；后续需求按当前工作流建立或更新活跃变更，不随意重写历史。
- 用户授权归档并同步时，先验证主规格同步成功，再归档。
- 仅修改任务涉及的代码、测试及必要文档。保护数据库、环境文件和凭据；调试输出中不要泄露秘密。
- 默认不提交、推送或部署，除非用户已授权。调试产物使用临时目录或 `output/playwright/`，交付前检查工作区。
